{
  "name": "genelab-root",
  "version": "2.0.0",
  "private": true,
  "description": "Root package config for Vercel deployment",
  "scripts": {
    "build": "cd backend && npm ci --only=production && echo 'Backend build completed successfully'",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
