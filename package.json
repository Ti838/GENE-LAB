{
  "name": "genelab-root",
  "version": "2.0.0",
  "private": true,
  "description": "Root package config for Vercel deployment using workspaces",
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "build": "echo 'Build completed'",
    "dev": "npm run dev --workspace=backend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
